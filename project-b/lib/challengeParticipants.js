// Moved to lib/challenges/participants.js (and merged with the superset
// version that had drifted in the standalone Traitors app — see that
// file's comment) — this file is kept as a re-export shim so none of
// this app's existing import sites needed to change.
export * from "./challenges/participants";
